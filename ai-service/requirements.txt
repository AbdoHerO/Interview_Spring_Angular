# --- web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# --- async / http ---
httpx>=0.27,<1.0
tenacity>=9.0,<10.0
aiofiles>=24.1
websockets>=13.0,<16.0  # client used by realtime voice gateway

# --- AI providers (SDKs are optional; we call HTTP directly to stay lean) ---
openai>=1.51          # used for OpenAI + Voyage fallback embeddings
anthropic>=0.34       # Claude
# Groq is OpenAI-compatible, called via openai SDK with custom base_url

# --- Vector DB ---
qdrant-client>=1.12,<2.0

# --- parsing ---
pymupdf>=1.25.0       # 1.25+ ships Python 3.14 wheels
python-docx>=1.1
markdown-it-py>=3.0
beautifulsoup4>=4.12
lxml>=5.3
chardet>=5.2

# --- repo handling ---
gitpython>=3.1

# --- tokenization / chunking ---
tiktoken>=0.8

# --- utils ---
python-dotenv>=1.0
orjson>=3.10
loguru>=0.7
